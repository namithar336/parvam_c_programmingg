#include<stdio.h>
int main(){
    FILE *fptr;
    fptr=fopen("test2.txt","r");
    if(fptr!=NULL){
        printf("file open successful");
    }
    else {
        printf("not successful");
    }
    return 0;
}